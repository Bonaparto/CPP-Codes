#include <iostream>
using namespace std;
int main(){

    int x = 1;
    x = 1 + x;
    x++;
    // инкремент
    ++x; // например y = ++x (сперва складывается и потом y принимает этот x)
    x+=;
    a = x++ + ++x // a = 1 + 3