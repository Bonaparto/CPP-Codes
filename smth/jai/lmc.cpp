#include <iostream>
using namespace std;
int main(){
    int x,y;
    cin >> x >> y;
    if(x > y){
        while(y % x != 0){y += b};
        cout << y;
    }
    if(x < y){
        while(y % x != 0){y += b};
        cout << y;
}