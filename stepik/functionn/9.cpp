#include <iostream>
#include <cmath>
using namespace std;

bool IsPrime(int n){
    for(int i = 2; i <= sqrt(n); ++i){
        double
        if()
    }
}

int main(){
    int n;
    cin >> n;
    cout << (IsPrime(n)? "YES" : "NO";
}