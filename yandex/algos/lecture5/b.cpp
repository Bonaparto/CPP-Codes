#include <iostream>
using namespace std;
int main(){
    int n, k, l = 0, r = 0, ans = 0, sum = 0;
    cin >> n >> k;
    int a[n];
    for(int i = 0; i < n; ++i) cin >> a[i];
    sum = a[0];
    while(l != n)
    {
        
    }
    cout << ans;
}